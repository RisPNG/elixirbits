<Layouts.app flash={@flash} content_class="mx-auto w-full max-w-md">
  <section class="rounded-lg border border-base-300 bg-base-100 shadow-sm">
    <div class="flex flex-col p-6 gap-6">
      <.header>
        Register
        <:subtitle>Create an account with your email and a password.</:subtitle>
      </.header>

      <.form
        for={@form}
        id="register-form"
        action={~p"/auth/user/password/register"}
        method="post"
        phx-change="validate"
        phx-submit="submit"
        phx-trigger-action={@trigger_action}
      >
        <.input field={@form[:email]} type="email" label="Email" required />
        <.input field={@form[:password]} type="password" label="Password" required />
        <.input
          field={@form[:password_confirmation]}
          type="password"
          label="Confirm password"
          required
        />

        <div class="flex flex-col mt-4">
          <.button type="submit" variant="primary" phx-disable-with="Registering...">
            Register
          </.button>
        </div>
      </.form>

      <p class="text-sm text-base-200-content">
        Already have an account?
        <.link navigate={~p"/sign-in"} class="text-primary hover:underline">Sign in</.link>
      </p>
    </div>
  </section>
</Layouts.app>
